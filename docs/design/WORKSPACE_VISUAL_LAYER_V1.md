# Workspace Visual Layer v1

Status: active Figma visual layer / implementation blocked  
Date: 2026-07-07  
Branch: `design-audit-v1`

## Executive summary

This document records the first application of GeoAI Brand Foundation v1 and Design System Components v1 to the current product workspace geometry.

The work is design-only and preserves the existing `/workspace` product structure. It does not approve implementation or Codex handoff.

## Figma

Page:

`01 — Product Design`

Current frame:

`Workspace Visual Layer v1.0 / current`

Previous product-design versions were moved left before creating the new current frame, following the version lane policy.

## Current screens created

The current v1.0 frame includes:

- Workspace / Map-first setup v1.0;
- Workspace / Selected point v1.0;
- Workspace / Analysis result v1.0;
- Projects / Readiness visual layer v1.0;
- Clickable prototype flow / QA reference.

## UX geometry preserved

The design preserves:

- TopNavigation;
- main canvas;
- 380px right AnalysisPanel;
- map-first setup;
- B2B / B2C tabs;
- project block;
- role / scenario block;
- map-first / criteria-first control;
- custom query;
- candidate search;
- selected point state;
- panel footer CTA;
- analysis / compare / report as main canvas states;
- Projects / Readiness source-lineage treatment.

## Visual layer applied

The visual layer applies:

- GeoAI Light neutral surfaces;
- Geo Teal active controls;
- Signal Blue primary CTA;
- validation amber for caveats and gaps;
- compact card rhythm;
- source labels;
- readiness table styling;
- caveat visibility near decision output;
- light, decision-oriented map styling.

## Prototype flow

Visible scenario flow included:

1. Map-first setup.
2. Select point / AOI / candidate.
3. Run Express Analysis from right panel.
4. Main canvas becomes Analysis / Compare / Report.
5. Projects / Readiness keeps source lineage visible.

Native Figma click links still require manual verification before the design can be called a fully clickable prototype.

## Data honesty

Required caveat remains:

“Screening hypothesis; official validation required; not a legal, cadastral, zoning, planning or valuation conclusion.”

## Decision

No implementation handoff is approved. Owner visual review and clickable prototype verification are required before any Codex prompt.
