# Create programme live harness receipt

Status: OFFLINE-VALIDATED TEST HARNESS; no live request or product change was made.

Six ordinary, non-frozen live scopes cover every current Create programme on two distinct synthetic AOIs at real Dubai locations:

- `dubai-create-rm-rectangle`, `dubai-create-rm-concave`
- `dubai-create-ch-rectangle`, `dubai-create-ch-concave`
- `dubai-create-cg-rectangle`, `dubai-create-cg-concave`

`rectangle` is the shared Business Bay AOI. `concave` is the shared large-L Al Khairan analogue. Each scope permits exactly one Standard Create POST with the existing USD0.30 reservation, validates the exact programme/AOI/controls before reservation, and keeps the existing telemetry, unknown-charge stop, source-context contract, A/B geometry, real MapLibre 2D/3D, local save/reopen and zero-replay checks. These are current-runtime functional journeys, not frozen-snapshot benchmark evidence.

Root's private bridge must add these six exact scope strings to its allowlist; no wildcard. Existing exact Preview SHA/host, protected deployment receipt, active synthetic persona, ledger headroom, approval string, cleanup and terminal receipt requirements remain unchanged. Run one scope at a time and settle it before selecting the next.
