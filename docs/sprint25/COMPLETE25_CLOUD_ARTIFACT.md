# COMPLETE25 — one original A09 browser artifact to cloud

Change request: connect the exact browser-generated A09 Dubai Hills Mall saved analysis to the existing nonpaid cloud save/reopen acceptance lane. Preserve the original artifact, paid/source gates, and all legacy v1/copy/continuation behavior. No new AI generation, data acquisition, hosted operation, Auth mutation or deployment is performed by this implementation.

Root reports founder approval for **one new A09 analysis save**, not reuse of the consumed historical marked-copy authority. Exact target: geoai-dev `pphdqkurxneyagvnnjdt`; organization `e80f85e4-7bc9-4201-ac28-58ed39f79271`; project `3c6b8bb6-57e4-46ac-b504-6c0e58c6b9dd`; key `dubai-investment-screening-demo`. Root owns verification, activation, backup guard, browser execution and cleanup. This document is not a live acceptance receipt.

The input is captured from the actual case browser before logout/context destruction. It is not reconstructed from the sanitized analysis evidence. The source is exactly `way/797700047`; case A09; EN/Dubai; Standard/custom; original IDs, labels, question, timestamps, selection and analysis remain unchanged.

## Contract and operator inputs

The dedicated capture helper is owned by the batch worker. It validates the original wire response against the frozen case before capturing the canonical browser artifact. The cloud adapter verifies that capture against the root-selected frozen manifest and repeats canonical product parser/checksum/telemetry verification in the browser harness.

Both inputs must be exact absolute real paths, regular single-link files with mode 0600 inside real mode-0700 directories owned by the current operator. SHA-256 is checked before parsing. Files are read-only; no normalization or rewrites are persisted. Never export a whole localStorage store containing user identity or session data.

The new schema is accepted only for new-artifact writer mode on the exact current Preview/commit. Historical source overrides, marked-copy settings, continuation, other cases and other source identities are rejected. The original v1 path remains separate.

Operator binds these four settings from the actual capture, never the template:

- `GEOAI_QUALITY20_CLOUD_ARTIFACT_PATH`: exact private `A09-browser-artifact.json`.
- `GEOAI_QUALITY20_CLOUD_ARTIFACT_SHA256`: its original captured file hash.
- `GEOAI_COMPLETE25_CLOUD_MANIFEST_PATH`: exact original batch `09-manifest.json`.
- `GEOAI_COMPLETE25_CLOUD_MANIFEST_SHA256`: original byte hash recorded in the capture envelope; whitespace edits change this hash.

The manifest settings are paired, required for this new schema, forbidden with legacy artifacts, and forwarded only to the writer browser. Synchronous preflight checks the envelope/frozen intent/public-data structure; the browser independently re-reads both files and awaits the full canonical saved-artifact parser/checksum/request/telemetry validation before Save. No historical/copy/continuation environment setting may be present, even empty. No new operator SQL/lifecycle behavior was added.

Saved-source acquiredAt is historical provenance, not a current source lease. Reopening the saved result one day later passes the same validation without a source or AI request. `responseHash` is the original wire-response provenance from capture; it is not falsely recomputed from the normalized saved projection. The unchanged capture-file SHA protects all original metadata, including IDs/label/timestamps; payload checksum separately protects the product operation.

Cloud save remains a separately gated root operation. Keep the existing exact Auth/cloud approval and fresh root-verified restorable backup receipt. Do not substitute an older restore timestamp or replay retired batch personas. One save must produce HTTP201; new-context import must equal the complete original artifact, reopen the result, and make zero paid AI calls. Outsider/viewer denial and unconditional scope/membership/session cleanup remain required.

## Verification scope

Offline fixtures prove parser, manifest, mode, privacy and immutability guards only. They do not prove the external source, OpenAI, cloud permissions, backup freshness, hosted save or browser reopen. Root integrates both capture and adapter commits before running the exact-chain acceptance.

Node24 check: `node --experimental-transform-types scripts/complete25-cloud-artifact-input-check.mjs` — 44 checks PASS, including full async product validation, paired settings, legacy mixing, source/case/deployment/request/result drift, original metadata file hash, symlink/hardlink/permissions/size rejection, exact writer forwarding, one-day-later saved reopen and unchanged original bytes. Optional argument is a read-only dependency checkout for pre-integration testing, not an execution target.

Adjacent checks: capture helper 58 PASS; existing cloud artifact/copy 23 PASS; existing cloud acceptance reporter/lifecycle/cleanup contract PASS. No network or paid calls. Whole-tree `tsc --noEmit --incremental false` currently reports only the pending parallel `captureNight21AnalysisDashboard` export dependency; root must integrate that dependency and rerun lint/types/build before final Preview. Production build/browser hosted acceptance are root-owned, not asserted here.

Ownership extension approved by root: `scripts/sprint10-cloud-live-acceptance.mjs` only paired-manifest validation and writer environment forwarding. No SQL, Auth, paid gate, cleanup, project tuple or lifecycle edits.
