# COMPLETE26 — focused-answer correction and preserved failure history

## Change request and observed failure

On 26 September the real batch on `fceccdb9e885b005d66771d4d6be711ff7faf28e` stopped at A01-S. A01-Q and A01-D passed on the same frozen object evidence; Standard returned HTTP 502 after both model attempts failed `focused_answer_shape`. The exact shape subpredicate cannot be reconstructed from the old generic diagnostic. Both synthetic personas were retired with read-back. The batch remains **FAIL, 2/58**, not partial release acceptance.

## Correction

- V13 specifies the existing focused-answer length, unique references/codes and status rules before generation. The JSON schema bounds ordinary prose; normalized UTF-16 length and all source/factual conditions remain server-authoritative. Direct-attribute and typed climate contracts remain distinct.
- Eighteen fixed diagnostic codes identify which existing shape predicate failed without logging provider prose or secrets. No additional retry, fallback fact or weaker validator is introduced.
- Current live responses require V13; saved V12 responses remain readable with their original version, receipt and evidence. Historical compatibility is not authorization to dispatch V12.
- Full, valid failure telemetry can settle cost for the exact `AI_OUTPUT_INVALID`/502 envelope. This never passes the result or scenario. Incomplete telemetry still stops dispatch as unknown.
- Existing founder standing authority can account a full prior reservation through a separate root-only, cause-reviewed application. The original unknown receipt remains unchanged; approval time is not backdated to the application time. All costs remain within the same USD15 ceiling.
- A separate explicit successor transition archives the failed candidate's three attempts and all receipts. It requires a new exact candidate, actual CI/protected Preview proofs, retired predecessor personas, unchanged complete matrix and exclusive execution. The previous two passes do not count toward new acceptance.

## Required verification

Synthetic shape/boundary, legacy restore, error-usage, atomic accounting and successor-negative checks are wired into CI. Local tests are not real API acceptance. Fresh exact-candidate browser/CI, all 58 live cases, useful content/visual review, cloud persistence and release gates remain required. Main and Production are unchanged at this correction.
